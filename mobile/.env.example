# Google OAuth Web client ID (public; safe to commit).
# CI reads this file via scripts/load-dart-defines.sh.
# Override locally by editing this file, or set GitHub secret GOOGLE_WEB_CLIENT_ID.

GOOGLE_WEB_CLIENT_ID=your-web-client-id.apps.googleusercontent.com
